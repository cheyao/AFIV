namespace ui {

void Preview::drawImage(void) {
        // The image we want to draw
        formats::Image* image = mImages[mTab];

        // Draw to: mViewPreview
        // Size: mViewPreview.w, mViewPreview.h
}

} // namespace ui
